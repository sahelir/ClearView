services:
  db:
    image: postgres:15
    container_name: clearview-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: clearview
    ports:
      - "5432:5432"
    volumes:
      - clearview_pgdata:/var/lib/postgresql/data

volumes:
  clearview_pgdata: